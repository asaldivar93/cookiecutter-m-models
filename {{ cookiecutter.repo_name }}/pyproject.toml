[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
[project]
requires-python = ">=3.10"
name = "mycobra"
version = "0"
description = "M-models dir"
license = "MIT"
license-files = ["LICENSE"]
dependencies = ["cobra", "plotly", "ipykernel", "numpy", "polars", "pandas", "networkx", "notebook"]

